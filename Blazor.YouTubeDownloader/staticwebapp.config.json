{
  "routes": [
    {
      "route": "/api/{*any}",
      "allowedRoles": [ "anonymous" ],
      "methods": [ "GET", "POST" ]
    },
    {
      "route": "/{*path}",
      "rewrite": "/index.html"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [ "/api/*" ]
  }
}